name: Deploy to Pages
on:
  push:
    branches: ["main"]
  workflow_dispatch:
permissions:
  contents: read
  pages: write
  id-token: write
concurrency:
  group: deploy
  cancel-in-progress: false
jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.publish.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/configure-pages@v5
      - name: Finalize
        env:
          ACCDDD: ${{ secrets.ACCDDD }}
          B8480B: ${{ secrets.B8480B }}
        run: |
          python3 -c "
          import pathlib, os, sys
          sec_a = os.environ.get('ACCDDD', '')
          sec_b = os.environ.get('B8480B', '')
          target = pathlib.Path('index.html')
          before = target.read_text(encoding='utf-8')
          after = before
          if sec_a:
              after = after.replace('<!-- 0a89ebf1de5d9e9833147398104a -->', sec_a)
          if sec_b:
              after = after.replace('<!-- 148f363978c543365d2defecc07c7dbea4b047cede46f6 -->', sec_b)
          if after == before and (sec_a or sec_b):
              sys.exit(1)
          if after != before:
              target.write_text(after, encoding='utf-8')
          "
      - uses: actions/upload-pages-artifact@v3
        with:
          path: .
      - id: publish
        uses: actions/deploy-pages@v4
